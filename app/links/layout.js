import "./links.css";

export const metadata = {
  title: "Link Saver — Morning Hub",
  description: "Save, summarize, and organize links with AI",
};

export default function LinksLayout({ children }) {
  return children;
}
